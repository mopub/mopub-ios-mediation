language: node_js
node_js:
- "8.16.0"
cache: npm
before_install:
  - git submodule add https://${GITHUB_TOKEN}@github.com/mopub/ios-mediation.git
  - git submodule update --init --recursive
  - npm i -g grunt-cli
  - npm i firebase-tools@3.17.2 -g
script:
  - chmod 777 ios-automate-release.sh
  - ./ios-automate-release.sh
# branch whitelist
branches:
  only:
  - Test-branch
